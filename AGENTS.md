# 에이전트 지침 — 족보 오리자보어 족보 재검증 도구

> 이 파일은 **AI 에이전트(Claude Code · Codex · Antigravity 등)가 읽는 운영 지침**입니다.
> 사람용 안내는 [README.md](README.md) 에 있습니다.
> 도구마다 읽는 파일명이 달라서 같은 내용이 `AGENTS.md` · `CLAUDE.md` 두 이름으로 들어 있습니다
> — **두 파일은 완전히 동일합니다.** 아무 쪽이나 읽으세요.

---

## 0. 기본 규칙

- **사람에게는 한국어로 말한다.** 사용자는 한국의 의대생이고, 코딩을 모릅니다. 명령어·경로·파일명은
  그대로 두되, 설명·질문·보고는 전부 한국어로 하세요.
- **사용자에게 명령어를 치게 하지 않는다.** 설치·실행·디버깅은 전부 에이전트가 합니다.
  사용자가 하는 일은 족보 PDF를 폴더에 넣는 것과, 체크포인트에서 "예"라고 답하는 것뿐입니다.
- **폴더 구조를 바꾸지 않는다.** `kd_library/build_viewer.py` 는 sibling `jokbo_pipeline/schema.py`
  를 import 합니다. 두 폴더는 반드시 같은 부모 아래 있어야 합니다.
- **족보 PDF와 결과물을 외부로 내보내지 않는다.** 커밋·업로드·공유하지 마세요.
- 사용자가 "시작해줘"라고만 하면, 폴더 안의 PDF를 찾아 과목명을 확인한 뒤 아래 흐름을 시작합니다.
- **스스로 범위를 줄이지 않는다.** 문항이 많으면 여러 턴에 걸쳐 나눠 하고 "계속"으로 이어갑니다.
  "불확실해서 이 문항들은 뺐습니다"로 끝내지 마세요 — 확인할 방법이 있으면 확인하고 전부 처리합니다.

---

## 1. 전체 흐름

```
PDF → [추출] → 체크포인트(사람 승인) → [블라인드 재검증] → questions_data.py → [검증] → [빌드] → DOCX + 뷰어
                                        ↑ 여기가 에이전트의 진짜 작업
```

### 0단계 — Python 확인 (먼저 할 것)

`main.py` 가 의존성을 설치해 주지만, **`main.py` 자체가 Python 으로 돌아갑니다.** Python 이
없는 컴퓨터에서는 이 순환을 에이전트가 먼저 끊어야 합니다.

```bash
python3 --version    # 없으면 아래로
```

| OS | Python 설치 |
|---|---|
| **Windows** | `winget install --id Python.Python.3.12 --source winget --scope user --silent --accept-package-agreements --accept-source-agreements` |
| **macOS** | 보통 이미 있습니다. 없으면 `brew install python` (brew 도 없으면 brew 먼저) |

Windows 에서 `python3` 대신 `python` 또는 `py` 만 있을 수 있습니다. 셋 다 시도해 보세요.
Python 이 확인된 뒤에 1단계로 갑니다.

### 1단계 — 추출

```bash
python3 main.py --pdf "<족보.pdf>" --subject "<과목명>" --extract-only
```

의존성 설치 → PDF 추출 → **체크포인트 결과 출력 후 종료**합니다.
`--subject` 는 폴더·파일 이름에 쓰이므로 사용자에게 확인받으세요 (예: "○○과").

산출물은 `output/<과목>_work/` 에 생깁니다:

| 파일 | 내용 |
|---|---|
| `all_questions.json` | **블라인드 문항** — stem·보기만. 원본 답·해설 **없음** |
| `answer_key.json` | **봉인 정답키** — idx + 원본 답 + 원본 해설 |
| `counts.json` | 추출 경고 매니페스트 (기계 판독용) |
| `structure.json` | 페이지·블록 구조 |
| `lines.json` | 라인 인덱싱 원문 (답 포함 — Phase2 Segmenter 입력용) |
| `images/` | 추출된 도판 |

### 2단계 — 체크포인트 (사람 승인 필수) ★

**승인은 터미널이 아니라 당신과 사용자의 대화에서 받습니다.**

`--extract-only` 없이 그냥 돌리면 `main.py` 가 `input()` 으로 사람 입력을 기다리는데,
코딩 에이전트의 셸 실행은 비대화형인 경우가 많아 거기서 막힙니다. 그래서 승인 절차가
두 단계로 나뉘어 있습니다:

1. **1단계의 `--extract-only` 출력을 읽고**, 사용자에게 **한국어로 풀어서** 설명하세요.
   화면 용어는 개발자용이라 그대로 보여 주면 안 됩니다. 이렇게 번역해 주세요:

   | 화면 | 사용자에게 |
   |---|---|
   | 답마커 (문항) | 찾은 문제 수 |
   | 문제번호 기준 | 문제 번호로 세어 본 수 (이 둘이 많이 다르면 추출이 샌 것) |
   | 헤더 (범위) | 나뉜 범위(단원) 수 |
   | 흡수 의심 문항 | 두 문제가 하나로 합쳐졌을 수 있는 건수 |

   그리고 **첫 3문항 미리보기가 실제 문제처럼 보이는지** 같이 확인해 달라고 하세요.
   문항 수가 예상과 크게 다르거나 미리보기가 깨져 있으면 그것부터 말해 주세요.

2. 사용자가 "맞다"고 하면 **그때** 승인 플래그를 붙여 재실행합니다:

   ```bash
   python3 main.py --pdf "<족보.pdf>" --subject "<과목명>" \
     --skip-extract --approve-extraction
   ```

**`--approve-extraction` 은 "사람이 이미 승인했다"는 선언입니다.** 사용자에게 실제로 묻지
않고 붙이지 마세요 — 이 도구의 존재 이유가 사라집니다.

⚠ **blocker 경고는 `--approve-extraction` 으로 뚫리지 않습니다.** 헤더 0개, 분절 누출 같은
건 승인과 무관하게 막힙니다. 그건 추출을 고쳐야 하는 문제지 승인할 문제가 아닙니다.

승인 뒤 `main.py` 는 `questions_data.py` 가 아직 없으므로 **안내를 출력하고 종료합니다.
이건 에러가 아니라 정상 동작**입니다 — 여기서부터 에이전트가 재검증을 합니다.

**추출이 이상할 때 (문항 수가 터무니없거나, 앞 문제 해설이 다음 문제에 딸려 오거나, 유령 문항이 있을 때):**

```bash
# 1순위 카드 — 포맷을 입력에서 자동 추론해 재추출
python3 main.py --pdf "<족보.pdf>" --subject "<과목명>" --auto-format --extract-only
```

문항이 **0개**면 텍스트 레이어가 없는 스캔 PDF일 가능성이 높습니다 → OCR 먼저.
범위 헤더가 안 잡히면 `--header-regex` 로 직접 지정합니다.

### 2.5단계 — 그래도 안 되면: LLM Segmenter 서브에이전트 (Phase2)

`--auto-format` 이 `#P1 … 미검출 → 기본 분절 사용` 을 찍고 결과가 여전히 지저분하면
(규칙적인 답·해설 구분자가 아예 없는 freeform 합본·학생 인라인 코멘트형 족보),
**의미 판단이 필요한 단계**입니다. 이때만 Segmenter 를 씁니다.

1. **별도 서브에이전트**를 띄웁니다. 그 서브에이전트에게만 `output/<과목>_work/lines.json`
   (라인 인덱싱 원문 — 답 포함)을 줍니다.
2. 지시: *"모든 라인을 역할별로 분류해서 **포인터맵만** 내보내라. 본문 텍스트는 한 글자도
   싣지 마라."* 반환 스키마:

   ```json
   {"questions":[{"qid":1,"stem_lines":[12,13],"option_lines":[14,15,16,17,18],
                  "answer_lines":[19],"explanation_lines":[20,21]}],
    "ignore_lines":[0,1,2]}
   ```

   정답·해설 라인은 **반드시** `answer_lines`/`explanation_lines` 로. 러닝헤더·표지·잡음은
   `ignore_lines` 로. 빈 줄이 아닌 모든 라인이 어딘가에 배정돼야 합니다(미할당은 결함).
3. 받은 JSON 을 `output/<과목>_work/segmentation.json` 으로 저장하고 **코드로 절단**합니다:

   ```bash
   python3 main.py --pdf "<족보.pdf>" --subject "<과목명>" \
     --segmentation "output/<과목>_work/segmentation.json"
   ```

   `extract.py` 가 포인터를 **원본 라인**에 적용하고(LLM 텍스트 재작성 0) 파티션·블라인드
   불변식·누출을 검증합니다. 실패하면 `SEGMENTATION_INVALID`/`SEGMENTATION_LEAK` (blocker)
   가 떠서 체크포인트가 막힙니다 → 해당 idx 의 라인 역할을 Segmenter 에게 다시 라벨하게 하고
   재시도합니다. **유한 재시도** — 무한 루프 금지.
4. 통과하면 `#P2 … ✓ 검증통과` 가 뜹니다. **체크포인트를 사람에게 다시 보여 주세요.**

세 단계 모두 실패하면, 체크포인트에서 사람에게 **솔직히** 말하세요 — "자동 추출로는 믿을 만한
결과가 안 나옵니다" 라고 하고 원본 대조·수동 보정을 안내합니다. **억지로 빌드하지 마세요.**

### 역할 격리 (블라인드의 근거)

블라인드는 분절기가 regex 냐 LLM 이냐로 정해지는 게 아니라, **정답을 본 역할의 출력을 Solver 가
소비하느냐**로 정해집니다. 세 역할을 분리하세요:

| 역할 | 누가 | 무엇을 보나 |
|---|---|---|
| **Segmenter** | 기본은 코드(`extract.py`). 막히면 **별도 서브에이전트** | `lines.json`(답 포함). **포인터맵만** 내보냄 |
| **Solver** | **새 서브에이전트** (Segmenter 와 분리) | `all_questions.json`(블라인드)만. 원본 PDF·`lines.json`·`answer_key.json`·Segmenter 대화 접근 금지 |
| **Comparer** | 메인 에이전트(나) | Solver 가 답을 낸 **뒤에만** `answer_key.json` 을 염 |

### 3단계 — 블라인드 재검증 ★

**이 도구의 존재 이유입니다. 아래 순서를 반드시 지키세요.**

1. **`all_questions.json` 만 열고 문제를 푼다.**
   **`answer_key.json` 을 먼저 열지 마세요.** 원본 답을 보고 나면 거기에 끌려가서(앵커링)
   재검증이 아니라 원본 답 추인이 됩니다. 그러면 이 도구를 쓸 이유가 없어집니다.
2. 다 풀고 **그때** `answer_key.json` 을 열어 대조하고, 문항마다 판정합니다:

   | 판정 | 언제 |
   |---|---|
   | `ANSWER_MATCH` | 내 답과 원본 답이 같음 |
   | `ANSWER_CORRECTED` | 원본 답이 틀렸다고 판단 — **근거를 해설에 명시** |
   | `ANSWER_DISPUTED` | 판단 불가·논쟁적·다른 선지도 참일 수 있음 |

   플래그 이름은 `jokbo_pipeline/schema.py` 의 `FLAG_TAXONOMY` 가 유일한 진실이다.
   이름을 틀리면 `validate.py` 가 '알 수 없는 flag' **경고**만 내고 통과시키므로(에러 아님)
   조용히 배지가 안 붙는다. 위 세 개는 **answer_group** 이라 **동시에 둘 이상 쓸 수 없다.**

   ⚠ 정답 번호를 바꾸기 전에 **6.2 를 먼저 읽을 것** — 의학적으로 틀려 보여도 답을
   바꾸면 안 되는 경우가 있다.

3. 결과를 `output/<과목>_work/questions_data.py` 로 작성합니다.
   - 스키마 틀: **`jokbo_pipeline/questions_data_template.py`** 를 복사해 채웁니다.
   - 필드 정의·규약 전체: **`jokbo_pipeline/README.md`** (필수/선택 필드, 복수정답 `answers`,
     R형, 다중 이미지 `images[]`, `【왕족】` 태그, 리치블록 5종, 범위 헤더 꼬리 규약).
4. **정답을 문두(stem)나 캡션에 흘리지 마세요.** 문제를 풀 수 없게 됩니다.
5. 문항이 많으면 **나눠서 진행하고 중간에 저장**하세요. `output/<과목>_work/` 에 남으므로
   다음 세션에서 이어서 할 수 있습니다.

### 4단계 — 검증

```bash
python3 jokbo_pipeline/validate.py --data "output/<과목>_work/questions_data.py"
```

스키마 필수필드 + 정답노출(no-spoiler) + 커버리지를 한 번에 검사합니다. 에러가 있으면 exit 1.
**빌드 전에 반드시 통과시키세요.** 더 엄격하게 보려면:

```bash
python3 jokbo_pipeline/validate.py --data "<...>/questions_data.py" \
  --raw "output/<과목>_work/all_questions.json" \
  --strict-detail --strict-meta --strict-scope
```

### 5단계 — 빌드

```bash
python3 main.py --pdf "<족보.pdf>" --subject "<과목명>" \
  --skip-extract --approve-extraction --final
```

`--skip-extract` 는 추출을 건너뛰고 DOCX와 HTML 뷰어를 만듭니다.
**`--final` 을 꼭 붙이세요** — 빌드 전 validate 를 `--strict-detail --strict-meta` 로
돌려 authoring 충실도(imp·오답선지 해설)와 교수명 규약을 강제합니다. 4단계를 건너뛰었더라도
최종본은 이 게이트를 통과해야만 나옵니다.

결과:
- `output/<과목>_재검증본.docx`
- `output/<과목>_뷰어.html` — 파일 하나로 완결. 더블클릭하면 열립니다.

빌드 후 사용자에게 두 파일 경로를 한국어로 알려 주고, 뷰어를 열어 보라고 안내하세요.

---

## 2. main.py 옵션

| 옵션 | 설명 |
|---|---|
| `--pdf` | 입력 족보 PDF (필수) |
| `--subject` | 과목명 (필수). 출력 파일·폴더 이름에 쓰임 |
| `--auto-format` | Phase1 포맷 자동 추론. 답·해설 블록 구분자·러닝헤더·컬럼을 입력에서 도출해 인라인 해설 침범·유령 문항을 구조적으로 제거. **추출이 지저분할 때 첫 번째 카드** |
| `--segmentation` | Phase2 에스컬레이션. LLM Segmenter 포인터맵(JSON) 경로. `--auto-format` 으로도 안 될 때만. 검증 실패 시 체크포인트가 막힘 |
| `--extract-only` | 추출까지만 하고 체크포인트 결과를 출력한 뒤 종료. **사람에게 묻지 않음** — 에이전트의 기본 진입점 |
| `--approve-extraction` | 사람이 이미 승인했다고 보고 체크포인트 프롬프트를 건너뜀. 실제로 승인받은 뒤에만 쓸 것. blocker 는 뚫리지 않음 |
| `--final` | 최종본 프로필 — 빌드 전 validate 를 strict-detail·strict-meta 로 강제 |
| `--skip-extract` | 추출 건너뛰고 빌드부터 (재검증 작성 완료 후) |
| `--skip-install` | 의존성 설치 건너뛰기 |
| `--dry-run` | 설치 명령만 출력하고 실행 안 함 |
| `--header-regex` | 범위 헤더 정규식 직접 지정 (족보 형식이 특이할 때) |
| `--work-dir` / `--out-dir` | 작업/출력 폴더 변경 (기본 `output/<과목>_work`, `output/`) |
| `--version` | 버전 출력 |

---

## 3. 구성 파일

```
main.py                  오케스트레이터 (설치 → 추출 → 체크포인트 → 빌드)
jokbo_pipeline/
  schema.py              ★스키마 단일 정의 — build.py·build_viewer.py·validate.py 가 import (복붙 금지)
  extract.py             PDF 구조 추출 (블라인드/정답키 분리)
  build.py               DOCX 빌더
  validate.py            통합 검증 게이트
  nospoiler_audit.py     정답 노출 감사
  coverage_audit.py      놓친 문항 커버리지 감사
  merge_rtype.py         왕족 변형 병합 → R형 초안
  format_lab.py          포맷 추론 (--auto-format)
  format_probe.py        포맷 진단
  llm_segment.py         Phase2 포인터맵 절단·검증 (--segmentation)
  parse_warnings.py      추출 경고 taxonomy·blocker 단일 진실
  questions_data_template.py   문항 스키마 템플릿
  README.md              ★파이프라인 상세 문서
kd_library/
  build_viewer.py        단일 HTML 뷰어 빌더
  viewer_template.html   뷰어 템플릿 (JS 로직 포함)
  README.md              ★뷰어 상세 문서
```

**스키마를 고칠 일이 있으면 `schema.py` 한 곳만 고칩니다.** 단, 뷰어 JS
(`viewer_template.html`)에 같은 로직이 있으므로 **양쪽을 함께** 맞춰야 합니다.

---

## 4. 테스트

```bash
python3 jokbo_pipeline/tests/test_builders.py
python3 jokbo_pipeline/tests/test_extract.py
python3 jokbo_pipeline/tests/test_extract_improvements.py
python3 jokbo_pipeline/tests/test_validate_integrity.py
python3 jokbo_pipeline/tests/test_llm_segment.py
python3 kd_library/tests/test_build_viewer.py
```

코드를 고쳤다면 관련 테스트를 돌려서 통과를 확인한 뒤 사용자에게 보고하세요.

---

## 5. 자주 나오는 상황

| 증상 | 대응 |
|---|---|
| 문항 수가 터무니없음 | `--auto-format` 으로 재추출 |
| 문제 앞에 앞 문제 해설이 붙음 | `--auto-format` (인라인 해설 침범 제거) |
| 문항 0개 | 스캔 PDF — OCR 먼저 |
| 범위 헤더가 안 잡힘 | `--header-regex` 지정 |
| `--auto-format` 도 지저분함 | 2.5단계 — LLM Segmenter 서브에이전트(`--segmentation`) |
| `SEGMENTATION_LEAK` blocker | 표시된 idx 의 정답/해설 라인이 stem 으로 라벨됨 → Segmenter 에게 재라벨 요청 |
| `SEGMENTATION_INVALID` blocker | 라인 배정에 중복·범위밖·미할당이 있음 → 포인터맵 재생성 |
| `questions_data.py 가 없습니다` | 정상. 3단계 재검증을 하라는 뜻 |
| `입력을 받을 수 없는 환경입니다(비대화형)` | `--extract-only` 를 안 붙였음. 2단계 절차대로 다시 |
| `python3: command not found` | 0단계 — Python 부터 설치 (`python`·`py` 도 시도) |
| 빌드 시 이미지 누락 경고 | `images/` 안 파일명과 데이터의 `image`/`images` 필드 대조 |
| 뷰어에서 문두 줄바꿈이 이상 | `viewer_template.html` 의 `.q-stem` 은 `white-space:pre-wrap` — 데이터의 개행을 그대로 살립니다 |

---

## 6. 재검증 실무 규칙 — 실제 작업에서 나온 것

앞의 절차를 지켜도 결과물이 시험에 못 쓰이게 되는 경우가 있습니다. 아래는 여러 과목을
실제로 재검증하면서 굳어진 규칙입니다. **판단 기준은 하나 — "이걸로 공부해서 시험을 잘 볼 수
있는가".** 자료로서 그럴듯한지가 아닙니다.

### 6.1 이미지 — 추측하지 말고 직접 보고 붙인다

`extract.py` 가 달아 주는 `candidate_images` 는 **y좌표로 추정한 후보일 뿐**입니다. 그대로
믿고 넣으면 엉뚱한 문항에 붙습니다. **페이지를 렌더해서 눈으로 확인하세요.**

```bash
# 한글 디지털 PDF는 110 dpi면 판독 충분. 스캔본은 300 dpi.
pdftoppm -png -r 110 -f <N> -l <N> -singlefile "<족보.pdf>" <출력이름>
```

⚠ **`pdftoppm`·`pdftotext` 는 poppler 도구입니다.**
- **macOS**: `install_deps` 가 `brew install poppler` 로 깔아 줍니다.
- **Windows**: 설치 목록에 **없고 패키지에도 동봉돼 있지 않습니다.** 필요하면 에이전트가
  런타임에 poppler Windows 빌드를 받아 `poppler_win/` 에 풀고 그 `bin/` 을 PATH에 넣으세요
  (`poppler_win/` 은 `.gitignore`·배포 제외 대상이라 다시 배포되지 않습니다).
  poppler 없이 가려면 `pypdf`/`Pillow` 로 페이지를 렌더하는 대체 경로를 쓰세요.

⚠ **`candidate_images` 는 `pdfplumber` 가 있어야 채워집니다.** 없으면 추출 로그에
`#4 이미지 매핑: pdfplumber 미설치 → 비활성` 이 뜨고 후보가 비므로 이미지 배정이 전부
수동이 됩니다. 이미지가 많은 족보라면 `pip install pdfplumber` 를 먼저 하세요
(기본 설치 목록에는 없습니다).

페이지를 보면 그림이 **어느 문항 지문 아래에 있는지** 함께 보이므로 귀속이 바로 확정됩니다.
여러 장을 한꺼번에 대조할 때는 PIL로 **콘택트 시트**(썸네일 + 파일명)를 만들어 한 번에 보세요 —
파일을 하나씩 여는 것보다 훨씬 쌉니다.

- **"귀속이 불확실해서 뺐습니다"는 결론을 내지 마세요.** 확인할 방법이 있으면 확인하고 전부
  처리합니다. 그래도 못 붙이는 것만 **사유를 한 줄씩** 남깁니다(재출제 중복 / 정답 노출 / 대응 문항 없음).
- **원본 시험지가 있으면 도판은 페이지 렌더에서 직접 자릅니다.** 족보의 문항 단위 크롭은 번호가
  어긋나 있거나 도판이 잘려 있는 경우가 있습니다. 페이지 PNG에 10% 격자를 그려 좌표를 읽고
  비율로 자르는 방식이 가장 빠르고 정확합니다.
  ⚠ **자를 때 아래쪽 선지가 딸려 들어가면 정답이 노출됩니다.** 경계를 꼭 확인하세요.
- **도판은 재압축해서 넣습니다.** 뷰어가 이미지를 base64로 인라인하므로 원본을 그대로 넣으면
  HTML이 수십 MB가 됩니다. **긴 변 1200~1400 px, JPEG q80~82** 로 줄이세요(실제 사례: 41 MB → 5.9 MB).
- 파일명 zero-padding 주의(`p8_` vs `p08_`). 연결한 뒤 참조 무결성을 스크립트로 확인하세요.

### 6.2 사진이 붙은 문항 — 판독 과제를 지우지 말 것

사진을 붙였으면 **그 사진을 읽는 것이 문제**입니다. 그걸 글자로 풀어 주면 문항이 죽습니다.

- **문두를 원본 그대로 둡니다.** "다음 사진의 질환은?" 을 "관절연골이 소실되고 연골하골이
  다져져 있으며…" 로 풀어 쓰지 마세요 — 사진을 붙이는 순간 그 서술이 정답 단서가 됩니다.
  소견 서술은 `new_expl`(해설)로 옮기면 학습 정보는 그대로 남습니다.
- **`image_caption` 을 달지 않습니다.** 캡션은 사진 바로 아래 렌더되는데, 사진이 눈앞에 있으니
  무엇인지 적어 줄 이유가 없습니다. 출처·연도가 필요하면 `source` 에 적으세요.
- **선지도 이미지를 해설하면 안 됩니다.** 사진 보기 문항은 `① 사진 1 … ⑤ 사진 5` 로 두고
  검사명은 해설로. 도해의 구간을 묻는 문항은 `① A … ④ D` 로 두고 이름은 해설로.
  보기 그림과 선지 개수·순서가 어긋나면 그림을 빼고 선지를 이름으로 정리합니다.

**붙이면 안 되는 그림 — 판단 기준은 "이 그림이 선지 중 하나를 글자로 알려 주는가":**

| 유형 | 예 |
|---|---|
| 구조 이름이 인쇄된 해부 도해 | 안구운동근 그림에 `Trochlear nerve (IV)` 라벨 → 답이 그대로 노출 |
| 검사명이 찍힌 사진 | 검사 장면에 `Electrodes for cVEMP` |
| 진단명이 인쇄된 해설 슬라이드 | 강의록 캡처에 정답 목록이 통째로 |
| 기전 도해 | 답에 해당하는 경로가 그려져 있음 |

**복원자가 "관련 사진 첨부합니다" 하며 덧붙인 교과서 그림·강의 슬라이드는 stem 이미지가 아닙니다.**
족보 PDF에서 뽑힌 도판에는 이런 게 섞여 있습니다. 같은 도판이 두 번 붙었으면 한 장만 남깁니다.

**이미지 플래그를 정확히 씁니다:**

- `STEM_IMAGE_A_PRESENT` 를 붙였으면 **조립 후 실제로 사진이 붙었는지 확인**하세요.
  (`validate.py` 가 플래그만 있고 `image`/`images` 가 없으면 **에러로 막습니다**.)
- 있었어야 할 도판을 못 구했으면 `IMAGE_MISSING` + `image_missing`(그 사진이 무엇이었는지 한 줄)로
  바꿔야 뷰어·DOCX에 `[이미지 누락]` 으로 렌더됩니다. 설명을 빠뜨리면 `validate.py` 가 경고합니다.
- ⚠ **애초에 도판이 없던 문항에는 `IMAGE_MISSING` 을 붙이지 않습니다.** 그 플래그는 "있었어야 할
  걸 못 구했다"는 뜻이라, 순수 텍스트 문항에 붙이면 거짓말이 됩니다. (실제로 도판이 0장인
  과목도 있습니다.)

### 6.3 정답을 함부로 바꾸지 말 것 ★

**재검증에서 의학적 오류를 찾았더라도, 문항 맥락상 출제의도에 맞는 답이 기존 정답이면
번호를 바꾸지 않습니다. 해설만 고칩니다.**

이 자료의 목적은 시험을 잘 보는 것입니다. 재출제되면 **답안지가 인정할 번호가 정답**입니다.
국제 표준이 강의와 다르다는 이유로 번호를 바꾸면 오히려 틀립니다.

출제의도의 근거로 쓸 것: 족보 복원자의 진술("2번이 확실히 맞았습니다"), 같은 강의의 다른 연도
문항에서 확인되는 용어 용법, 강의록·PPT 인용.

**해설 서술 형식을 통일하세요:**

> 강의·병원 기준을 먼저 쓰고 → 현재의 국제 기준을 괄호나 별도 문단으로 덧붙이고
> → **"시험에서는 ○를 고른다"** 로 닫는다.

- **기관 규정을 보편 기준처럼 쓰지 마세요.** 사건등급 체계, 보고 기한, 낙상 위험 연령 경계,
  약물 사용 권한 같은 것은 병원마다 다릅니다 — "이 병원·이 강의 기준"이라고 못 박으세요.
- 다른 선지도 참일 수 있으면 그 이유를 본문에 적고 `ANSWER_DISPUTED` 로 바꿉니다
  (DOCX·뷰어의 FLAGS 줄에 표시됩니다).
- 해설에서 고쳐야 할 전형: 낡은 수치, 절대명제("~는 그 자체로 ~이다", "예방 대책이 없다"),
  기한 오독(분석·계획 제출 기한을 개선활동 완료 기한으로 읽는 것).

**원본 시험지가 있으면 그게 정본입니다.** 선배들이 기억으로 복원한 족보와 어긋나면 **원본을
따르고** 차이는 `scope_note` 에 남깁니다. 실제로 복원본에서 환아 나이(6세 ↔ 6개월), 사진 설명,
정답 위치가 틀린 사례가 여럿 나왔습니다.

- 원본에도 인쇄 오식이 있습니다(문두 중복, 선지 ①②가 같은 문장). 오식으로 보고 복원하되
  `scope_note` 로 밝힙니다.
- 전사본에 선지가 빠져 있으면 **지어내지 말고** 원본 페이지를 이미지로 읽어 채웁니다.
- 정답 기록이 없거나 문두-선지가 어긋나 확정할 수 없으면 `ANSWER_DISPUTED` + 판단 근거.

### 6.4 데이터 작성 함정

- **해설 본문에 마크다운을 쓰지 마세요.** `build.py`(DOCX)도 `viewer_template.html`(뷰어)도
  마크다운을 파싱하지 않아 `**강조**` 가 별표째 출력됩니다. 학명 이탤릭 `*Klebsiella*` 도 같습니다.
  강조는 문장 구조로 하세요. **별표는 내용일 때만** 씁니다(예: 치식 표기의 `*`/`**` — 마크다운으로
  오인해 일괄 제거하면 데이터가 깨집니다). `validate.py` 가 `**…**`·`*…*` 쌍을 경고로 잡아 주지만,
  단독 별표는 내용으로 보고 통과시킵니다.
- **복원 누락 선지는 번호를 비우지 말고 플레이스홀더로 채웁니다.** `options` 를 ①②③⑤ 처럼
  건너뛰면 `validate.py` 가 `verified 정답(5)이 선지 수(4) 초과` 에러로 잡습니다.
  `"④ (족보에 복원되어 있지 않음)"` 을 넣고 `recon` 에 사정을 적으세요.
  (`validate.py` 가 ①②③⑤ 같은 **마커 불연속**을 따로 경고합니다.) `--strict-detail` 을
  쓴다면 그 자리의 오답 해설도 한 줄 넣어야 통과합니다.
- **연도별로 반복되는 왕족 문항은 하나로 합치고 `years` 에 전 연도**를 적습니다. 선지 배치만
  바뀐 해는 `orig_ans` 에 `"답 4 (2025) / 답 1 (2023 — '경제적이다'가 ①에 배치)"` 식으로 병기합니다.
  (실제로 원문 ~185문항이 112문항으로 정리된 과목이 있습니다.)
- **같은 해 같은 문항을 두 사람이 각각 복원해 둔 경우**는 선지 문면과 답이 갈립니다.
  **합치지 말고 두 판본을 별도 문항으로 싣고** `tip` 에서 서로를 가리키세요.
- **중복 stem 경고는 대부분 의도된 것**입니다(같은 개념의 연도별 변형, 부록과 주제별 단위에
  같은 문항). 지우지 마세요.
- **`--strict-scope` 오탐**은 범위 헤더의 희귀 토큰 때문에 생깁니다. **문항을 옮기지 말고
  헤더 문구를 바꿔** 해결하세요.
- **문항이 많으면 쪼개고, 한 단위 끝날 때마다 `validate.py` → 빌드까지 돌립니다.** 마지막에
  몰아서 하지 마세요 — 어디서 깨졌는지 못 찾습니다.
- **대량 편집은 손으로 하지 말고 스크립트를 만드세요.**
   ⚠ 파이썬 `ast` 로 편집한다면 `col_offset` 은 **UTF-8 바이트 오프셋**입니다. 한글 파일에서
   문자 인덱스로 계산하면 어긋납니다.
- 범위 **제목** 문자열 변경은 qid 파괴(뷰어 진도·🚩 체크가 stem 기반 qid 로 저장되므로) — 꼬리(`· [2026 담당교수: …]`)만 바꾸는 것은 안전하다.

### 6.5 2단 조판 족보

`--auto-format` 의 컬럼 인지로 해결되지 않으면, 텍스트를 좌/우 단으로 **나눠서** 뽑습니다.
한 번에 뽑으면 두 단이 줄 단위로 뒤섞여 읽을 수 없습니다. A4(595×841 pt) 기준:

```bash
pdftotext -layout -f <N> -l <N> -x 0   -y 0 -W 300 -H 841 "<pdf>" left.txt
pdftotext -layout -f <N> -l <N> -x 300 -y 0 -W 295 -H 841 "<pdf>" right.txt
```

이 저장소의 공용 도구 python3 jokbo_pipeline/extract_cols.py --pdf "<pdf>" --out-dir <폴더> 가 같은 일을 한 번에 한다.
