# 족보 오리자보어 파이썬 의존성 — 단일 진실(single source of truth).
#
# main.py 의 install_deps() 가 `pip install -r requirements.txt` 로 이걸 그대로 설치한다.
# 패키지 이름을 main.py 에 복붙하지 말 것 — 예전에 windows/macos 두 곳에 같은 목록이
# 복붙돼 있었고, 그래서 한쪽에만 pdfplumber 가 빠지는 드리프트가 생겼다.
#
# ── 상한(<)을 두는 이유 ────────────────────────────────────────────────────────
# 예전엔 `pip install --upgrade <이름>` 으로 '그날의 최신'을 즉석 설치했다. 지금은
# 통과하지만, 나중에 상위 버전이 API 를 바꾸면 **그 시점 이후에 설치한 사람 컴퓨터에서만**
# 갑자기 깨진다. 배포판을 받는 사람은 그걸 디버깅할 수 없으므로 다음 major 를 막아 둔다.
# 검증 시점: 2026-08 (pypdf 6.12.2 · python-docx 1.2.0 · Pillow 12.2.0 · pdfplumber 0.11.9)
# 상한을 올릴 때는 ./run_tests.sh 를 돌려 통과를 확인하고 올릴 것.

# PDF 텍스트·이미지 추출 (extract.py 의 핵심 — 없으면 아무것도 안 된다)
pypdf>=5.0,<7

# DOCX 생성 (build.py)
python-docx>=1.1,<2

# 이미지 처리 — 뷰어 base64 인라인·도판 재압축 (build_viewer.py)
Pillow>=10.0,<13

# 문항별 후보 이미지 매핑(extract.py 의 y좌표 기하). 없어도 파이프라인은 돌지만
# candidate_images 가 비어 이미지 배정이 전부 수동이 된다 — 사진 있는 족보면 사실상 필수.
pdfplumber>=0.11,<0.12
